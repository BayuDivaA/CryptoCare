export const shortenAddress = (address) => `${address.slice(0, 5)}...${address.slice(address.length - 4)}`;
export const shortenAddress2 = (address) => `${address.slice(0, 7)}...${address.slice(address.length - 6)}`;
